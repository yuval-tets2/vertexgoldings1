/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { AdmissionBatchService } from "../admissionBatch.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AdmissionBatchCreateInput } from "./AdmissionBatchCreateInput";
import { AdmissionBatchWhereInput } from "./AdmissionBatchWhereInput";
import { AdmissionBatchWhereUniqueInput } from "./AdmissionBatchWhereUniqueInput";
import { AdmissionBatchFindManyArgs } from "./AdmissionBatchFindManyArgs";
import { AdmissionBatchUpdateInput } from "./AdmissionBatchUpdateInput";
import { AdmissionBatch } from "./AdmissionBatch";
import { AdmissionFormFindManyArgs } from "../../admissionForm/base/AdmissionFormFindManyArgs";
import { AdmissionForm } from "../../admissionForm/base/AdmissionForm";
import { AdmissionFormWhereUniqueInput } from "../../admissionForm/base/AdmissionFormWhereUniqueInput";
import { CourseFindManyArgs } from "../../course/base/CourseFindManyArgs";
import { Course } from "../../course/base/Course";
import { CourseWhereUniqueInput } from "../../course/base/CourseWhereUniqueInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class AdmissionBatchControllerBase {
  constructor(
    protected readonly service: AdmissionBatchService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: AdmissionBatch })
  @nestAccessControl.UseRoles({
    resource: "AdmissionBatch",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async create(
    @common.Body() data: AdmissionBatchCreateInput
  ): Promise<AdmissionBatch> {
    return await this.service.create({
      data: data,
      select: {
        createdAt: true,
        endDate: true,
        id: true,
        name: true,
        startDate: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [AdmissionBatch] })
  @ApiNestedQuery(AdmissionBatchFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "AdmissionBatch",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findMany(@common.Req() request: Request): Promise<AdmissionBatch[]> {
    const args = plainToClass(AdmissionBatchFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        createdAt: true,
        endDate: true,
        id: true,
        name: true,
        startDate: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: AdmissionBatch })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "AdmissionBatch",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findOne(
    @common.Param() params: AdmissionBatchWhereUniqueInput
  ): Promise<AdmissionBatch | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        createdAt: true,
        endDate: true,
        id: true,
        name: true,
        startDate: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: AdmissionBatch })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "AdmissionBatch",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async update(
    @common.Param() params: AdmissionBatchWhereUniqueInput,
    @common.Body() data: AdmissionBatchUpdateInput
  ): Promise<AdmissionBatch | null> {
    try {
      return await this.service.update({
        where: params,
        data: data,
        select: {
          createdAt: true,
          endDate: true,
          id: true,
          name: true,
          startDate: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: AdmissionBatch })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "AdmissionBatch",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async delete(
    @common.Param() params: AdmissionBatchWhereUniqueInput
  ): Promise<AdmissionBatch | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          createdAt: true,
          endDate: true,
          id: true,
          name: true,
          startDate: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/admissionForm")
  @ApiNestedQuery(AdmissionFormFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "AdmissionForm",
    action: "read",
    possession: "any",
  })
  async findManyAdmissionForm(
    @common.Req() request: Request,
    @common.Param() params: AdmissionBatchWhereUniqueInput
  ): Promise<AdmissionForm[]> {
    const query = plainToClass(AdmissionFormFindManyArgs, request.query);
    const results = await this.service.findAdmissionForm(params.id, {
      ...query,
      select: {
        admissionBatch: {
          select: {
            id: true,
          },
        },

        course: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        formSubmittedAt: true,
        id: true,

        student: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/admissionForm")
  @nestAccessControl.UseRoles({
    resource: "AdmissionBatch",
    action: "update",
    possession: "any",
  })
  async connectAdmissionForm(
    @common.Param() params: AdmissionBatchWhereUniqueInput,
    @common.Body() body: AdmissionFormWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      admissionForm: {
        connect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/admissionForm")
  @nestAccessControl.UseRoles({
    resource: "AdmissionBatch",
    action: "update",
    possession: "any",
  })
  async updateAdmissionForm(
    @common.Param() params: AdmissionBatchWhereUniqueInput,
    @common.Body() body: AdmissionFormWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      admissionForm: {
        set: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/admissionForm")
  @nestAccessControl.UseRoles({
    resource: "AdmissionBatch",
    action: "update",
    possession: "any",
  })
  async disconnectAdmissionForm(
    @common.Param() params: AdmissionBatchWhereUniqueInput,
    @common.Body() body: AdmissionFormWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      admissionForm: {
        disconnect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/courses")
  @ApiNestedQuery(CourseFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "Course",
    action: "read",
    possession: "any",
  })
  async findManyCourses(
    @common.Req() request: Request,
    @common.Param() params: AdmissionBatchWhereUniqueInput
  ): Promise<Course[]> {
    const query = plainToClass(CourseFindManyArgs, request.query);
    const results = await this.service.findCourses(params.id, {
      ...query,
      select: {
        comment: true,
        createdAt: true,
        credits: true,

        department: {
          select: {
            id: true,
          },
        },

        description: true,

        faculty: {
          select: {
            id: true,
          },
        },

        id: true,
        name: true,

        university: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/courses")
  @nestAccessControl.UseRoles({
    resource: "AdmissionBatch",
    action: "update",
    possession: "any",
  })
  async connectCourses(
    @common.Param() params: AdmissionBatchWhereUniqueInput,
    @common.Body() body: CourseWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      courses: {
        connect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/courses")
  @nestAccessControl.UseRoles({
    resource: "AdmissionBatch",
    action: "update",
    possession: "any",
  })
  async updateCourses(
    @common.Param() params: AdmissionBatchWhereUniqueInput,
    @common.Body() body: CourseWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      courses: {
        set: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/courses")
  @nestAccessControl.UseRoles({
    resource: "AdmissionBatch",
    action: "update",
    possession: "any",
  })
  async disconnectCourses(
    @common.Param() params: AdmissionBatchWhereUniqueInput,
    @common.Body() body: CourseWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      courses: {
        disconnect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }
}
